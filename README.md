# skills

A collection of my own personal Claude Code skills. These should go in `~/.claude/skills`.

Installing these on a new computer:

```sh
cd ~/.claude/
git clone git@github.com:stephenturner/skills.git
```